gunicorn==20.0.4

# We're waiting on Django 3 until it's further into the lifecycle.
Django==3.0.7
whitenoise==5.1.0

django-registration==3.1
# Haven't seen an update since 2018
django-cron==0.5.1
# Haven't seen an update since 2016
django-npm>=1.0.0,<1.0.99
django-watchman==1.1.1
django-su==0.9.0
# Haven't seen an update since 2016
django-settings-export==1.2.1
# Haven't seen an update since 2015
django-macros==0.4.0
django-debug-toolbar==2.2
django-mysql==3.7.1
# Force this version of promise because the newest version doesn't support dict
promise==2.2.1

# graphql
graphene-django==2.8.0
django-filter==2.3.0

# object-level permissions
rules==2.2
django-model-utils==4.0.0

# These should be okay to update minors
numpy==1.19.1
pandas==1.0.5

# requests>=2.22,<2.22.99
pyOpenSSL>=19.0,<19.0.99
# canvasapi>=0.10,<0.10.99
protobuf==3.12.2

# These caused problems in the past so test when updating
djangosaml2==0.19.1
pysaml2==5.1.0

SQLAlchemy==1.3.18
psycopg2==2.8.5
mysqlclient==1.4.6
google-cloud-bigquery==1.26.0

pinax-eventlog==2.0.3
ptvsd==4.3.2
django-ptvsd-debug==1.0.3
PyLTI1p3==1.7.0
django-webpack-loader==0.7.0
django-csp==3.6
